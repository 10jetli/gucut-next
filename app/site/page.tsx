'use client'
// ระบบหลังร้านของ "เว็บไซต์หน้าร้าน" (gucut.com) — มาแสดงในโดเมนเดียวกับหลังร้านตัวนี้
//
// ทำไมต้องฝังเป็นกรอบ (iframe) ไม่ใช้การดึงหน้ามาเสิร์ฟตรง ๆ
//   สองระบบนี้เป็น Next.js ทั้งคู่ และใช้เส้นทางไฟล์ /_next/ เหมือนกันเป๊ะ
//   ถ้าดึงหน้าจาก gucut.com มาเสิร์ฟใต้โดเมนนี้ ไฟล์ /_next/ จะไปชนกันจนพังทั้งสองฝั่ง
//   การฝังเป็นกรอบทำให้ข้างในยังวิ่งบนโดเมน gucut.com ตามเดิม ทุกอย่างจึงทำงานปกติ
//   ส่วนคนใช้เห็นเป็นระบบเดียวกัน ไม่ต้องกระโดดออกไปอีกเว็บ
//
// ⚠️ กรอบนี้ยังถูกป้องกันด้วยรหัสของหลังร้านตัวนี้อยู่ (middleware.ts คุม /site)
//    และข้างในยังต้องใส่รหัสหลังร้านของเว็บหน้าร้านอีกชั้นตามเดิม
const SRC = 'https://gucut.com/admin/'

export default function SitePage() {
  return (
    <div className="h-[calc(100vh-2rem)] w-full overflow-hidden rounded-xl border border-gray-200 bg-white">
      <iframe
        src={SRC}
        title="ระบบหลังร้านเว็บไซต์"
        className="h-full w-full border-0"
        // อนุญาตเท่าที่หน้าหลังร้านต้องใช้จริง (อัปโหลดสลิป · คัดลอกลิงก์ · เปิดแท็บใหม่)
        allow="clipboard-write"
      />
    </div>
  )
}
