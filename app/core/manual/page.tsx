// คู่มือการใช้งาน — แผ่นที่ 3 ของแผงตารางจุด 9 ช่อง (ZORT ก็มีแผ่นนี้)
//
// ⚠️ **ห้ามเขียนสิ่งที่ระบบยังทำไม่ได้ลงคู่มือ** คู่มือที่บอกวิธีทำสิ่งที่ยังไม่มี
//    แย่กว่าไม่มีคู่มือ เพราะคนใช้จะไล่หาปุ่มที่ไม่มีอยู่จริงแล้วสรุปว่าระบบพัง
//    ทุกบรรทัดในไฟล์นี้ตรวจกับโค้ดจริงแล้ว (app/core/pos · app/core/moves · app/core/stock)
// ⚠️ แก้ระบบเมื่อไหร่ต้องกลับมาแก้หน้านี้ด้วย — ไม่มีอะไรอัปเดตให้เอง
import Link from 'next/link'

function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <section className="bg-white border border-gray-200 rounded-md p-5 mb-3">
      <h2 className="text-[15px] font-semibold text-gray-900 mb-2.5">{title}</h2>
      <div className="text-[13.5px] text-gray-700 leading-[1.85] space-y-2">{children}</div>
    </section>
  )
}

/** ป้ายตัวอย่างให้เห็นของจริง ไม่ใช่บรรยายเป็นตัวหนังสือว่า "ป้ายสีเหลือง" */
function Badge({ cls, children }: { cls: string; children: React.ReactNode }) {
  return <span className={`inline-block text-[11.5px] font-semibold rounded px-2 py-0.5 ${cls}`}>{children}</span>
}

export default function ManualPage() {
  return (
    <div className="p-4 md:p-6">
      <div className="max-w-[760px]">
        <h1 className="text-[26px] leading-tight font-semibold text-gray-900">คู่มือการใช้งาน</h1>
        <p className="text-[13px] text-gray-500 mt-1 mb-4">
          ระบบหลังร้าน GUCUT · เขียนจากสิ่งที่ระบบทำได้จริงตอนนี้ ไม่ใช่สิ่งที่ตั้งใจจะทำ
        </p>

        <Section title="ระบบนี้คืออะไร">
          <p>
            เป็นหลังร้านของเราเอง <b>อ่านตัวเลขจากคลังของเราเอง ไม่ได้ดึงสดจาก ZORT</b> —
            คลังเราเก็บออเดอร์และภาพถ่ายสต็อกไว้เองทุกวัน
          </p>
          <p>
            ตอนนี้ <b>เดินคู่ขนานกับ ZORT</b> คือทำงานพร้อมกันทั้งสองระบบ แล้วเทียบทุกวันว่าตัวเลขตรงกันไหม
            จนกว่าจะพิสูจน์ได้ว่าตรงตลอด ถึงจะเลิกจ่าย ZORT ได้ —{' '}
            <Link href="/core" className="text-blue-600 hover:underline">ดูหน้าปัดเทียบยอด</Link>
          </p>
          <p className="text-gray-500">
            ระหว่างนี้ <b>ZORT ยังต้องใช้ได้ปกติทุกวัน</b> ห้ามหยุดใช้เพราะเห็นว่าระบบนี้ทำได้แล้ว
          </p>
        </Section>

        <Section title="งานที่ 1 — เปิดบิลขายหน้าร้าน">
          <p>
            เมนู <b>รายการขาย → สร้างรายการขาย (POS)</b> หรือกดตารางจุด 9 ช่องมุมขวาบนแล้วเลือก POS
          </p>
          <p>
            พิมพ์ชื่อหรือ<b>ยิงบาร์โค้ด</b>ในช่องค้นหา แล้วกด Enter จะหยิบตัวแรกลงตะกร้าให้เลย ·
            หรือกดเลือกจากแผงหมวดทางซ้ายก็ได้ · เลือกสาขาครั้งเดียว ระบบจำไว้ให้ ไม่ต้องเลือกทุกบิล
          </p>
          <p>
            ลูกค้าเดินไปหยิบของเพิ่ม กด <b>พักบิล</b> แล้วเปิดใบใหม่ได้เลย บิลที่พักไว้อยู่ล่างซ้าย
            <span className="text-gray-500"> (พักไว้ในเครื่องนี้เท่านั้น เปลี่ยนเครื่องแล้วไม่เห็น)</span>
          </p>
          <p>ป้ายใต้ชื่อสินค้าบอกเรื่องใบอนุญาต — <b>อ่านทุกครั้งก่อนรับเงิน</b></p>
          <ul className="list-none space-y-1.5 pl-0">
            <li><Badge cls="text-emerald-800 bg-emerald-100">ไม่ต้องขอใบอนุญาต</Badge> <span className="text-gray-600">ขายได้เลย</span></li>
            <li><Badge cls="text-amber-800 bg-amber-100">ต้องขอ ลซ.๒</Badge> <span className="text-gray-600">ลูกค้าต้องมีใบรับรองก่อน ร้านเก็บตอนกลางไว้เป็นหลักฐาน</span></li>
            <li><Badge cls="text-red-800 bg-red-100">ต้องตรวจสอบก่อน</Badge> <span className="text-gray-600">ระบบยังไม่รู้ว่ารุ่นนี้ต้องขอไหม — <b>อย่าเพิ่งขาย ถามก่อน</b></span></li>
          </ul>
          <p>
            ถ้าของที่หยิบมา<b>ราคา 0 บาท</b> ระบบจะเตือนทันทีที่เพิ่ม และแถวนั้นจะเป็นสีแดง
            ปิดบิลไม่ได้จนกว่าจะติ๊กยืนยันว่าตั้งใจขายราคานั้นจริง —
            กันการยิงบาร์โค้ดรัว ๆ แล้วได้บิล 0 บาทโดยไม่มีใครทันสังเกต
          </p>
        </Section>

        <Section title="งานที่ 2 — รับของเข้า / ปรับสต็อก">
          <p>
            เมนู <b>รายการซื้อ → รับสินค้า / ปรับสต็อก</b> · เลือกเหตุผลให้ตรง
            (รับเข้า · โอนเข้า · โอนออก · ปรับยอด · ของเสีย · รับคืน)
          </p>
          <p>
            <b>เลขอ้างอิงต้องกรอกทุกครั้ง</b> เพราะระบบใช้เลขนี้กันการบันทึกซ้ำ —
            กดปุ่มสองครั้งของจะไม่เข้าสองรอบ และถ้าใบนั้นเคยบันทึกไปแล้ว
            ระบบจะบอกตรง ๆ ว่า &quot;ใบนี้เคยบันทึกไปแล้ว&quot; ไม่ใช่ขึ้นเขียวหลอก
          </p>
          <p className="text-gray-500">บันทึกผิดใบไหน ลบได้ทีละใบจากตารางด้านล่างของหน้าเดียวกัน</p>
        </Section>

        <Section title="งานที่ 3 — ดูของใกล้หมด">
          <p>
            เมนู <b>สินค้า → สินค้า</b> แล้วเลือกแท็บ <b>ของหมด</b> หรือ <b>ใกล้หมด</b> ·
            ตัวเลขในวงเล็บบนแท็บนับจากทั้งคลัง ไม่ใช่แค่หน้าที่เห็นอยู่
          </p>
          <p>
            <b className="text-red-600">ตัวเลขสีแดง</b> คือคงเหลือติดลบ แปลว่าขายออกไปมากกว่าที่ระบบรู้ว่ามี —
            มักเป็นค่าบริการหรือของที่ยังไม่เคยรับเข้าระบบ ไม่ใช่ของหายเสมอไป
          </p>
          <p>
            อยากรู้ว่าต้องสั่งอะไรเพิ่ม ไปที่{' '}
            <b>วางแผนธุรกิจ → วางแผนสั่งซื้อซ้ำ</b> (ระบบสั่งของที่ร้านใช้อยู่แล้ว)
          </p>
        </Section>

        <Section title="สัญลักษณ์ที่ต้องรู้">
          <p>
            🔔 <b>กระดิ่งมุมขวาบน</b> = เรื่องที่ควรมาดู (ยอดไม่ตรง ZORT · สต็อกไม่ตรง · ยอด Shopee ไม่ตรง)
            · <b>ระบบไม่ตรวจเอง ต้องกดปุ่มตรวจ</b> และจุดแดงจะขึ้นเฉพาะตอนเจอเรื่องจริงเท่านั้น
          </p>
          <p>
            <b>เมนูที่แสดงจางและมีจุดเล็กท้ายชื่อ</b> = หน้านั้นมีในเมนูแล้วแต่ยังไม่ได้ทำเนื้อหา
            กดเข้าไปจะบอกว่าจะทำอะไร และตอนนี้ให้ไปทำที่ไหนก่อน
          </p>
          <p>
            <b>เวลาทุกที่ในระบบเป็นเวลาไทย</b> ถึงเซิร์ฟเวอร์จะรันด้วยเวลาสากลก็ตาม —
            ถ้าเห็นเวลาที่ดูแปลก ๆ ให้บอก อย่าเพิ่งเชื่อว่าเป็นเรื่องปกติ
          </p>
        </Section>

        <Section title="อะไรที่ยังต้องทำที่ ZORT อยู่">
          <p>
            <b>เพิ่มสินค้าใหม่</b> · สินค้าเป็นชุด · สินค้าหลากคุณสมบัติ · ใบเสนอราคา · แพ็คสินค้า ·
            กระเป๋าเงิน · กลุ่มลูกค้า · ตัวแทนจำหน่าย · กลุ่ม Lead Time
          </p>
          <p>
            <b>แชท Facebook กับ LINE</b> ยังตอบที่แอป ZORT Social Commerce ·
            แชท Shopee/Lazada/TikTok ยังอยู่ที่ Duoke ·
            ที่นี่มีเฉพาะข้อความที่ลูกค้าทักผ่านหน้าเว็บร้าน
          </p>
          <p className="text-gray-500">
            รายการนี้จะสั้นลงเรื่อย ๆ — กดเมนูที่ยังจางอยู่จะเห็นว่าหน้านั้นวางแผนไว้ว่าจะทำอะไร
          </p>
        </Section>

        <div className="flex flex-wrap gap-2 mt-4">
          <Link href="/core/pos" className="text-[13px] font-semibold text-white rounded-full px-5 py-2" style={{ background: '#1b3b73' }}>
            เปิดจอขายหน้าร้าน
          </Link>
          <Link href="/core" className="text-[13px] font-medium text-gray-600 bg-white border border-gray-300 rounded-full px-5 py-2 hover:bg-gray-50">
            ดูความคืบหน้าโครงการแก่น
          </Link>
        </div>
      </div>
    </div>
  )
}
