// ร้านค้าออนไลน์ → เซลเพจ — ผังจาก `zort-ui/10-ร้านค้าออนไลน์-เซลเพจ-ว่าง.jpg`
// ⚠️ ชื่อจอของ ZORT เป็นภาษาอังกฤษว่า "Sale pages" จริง ๆ (เมนูซ้ายเขียนไทยว่า เซลเพจ)
//    ใช้ชื่อตามจอจริงเพื่อให้คนที่ชิน ZORT จำได้ทันที
//
// 🔴 **เลข 0 ต้องบอกที่มา ไม่ใช่ประกาศเป็นข้อเท็จจริง** (แก้ 15 ก.ย. 2569)
//    เดิมส่ง sumLabel="มี 0 หน้า" ⇒ หัวจอประกาศว่า "ที่ ZORT เมื่อ 3 ก.ย. 2569 มี 0 หน้า"
//    ⇒ เป็นคำยืนยันที่ **เราพิสูจน์เองไม่ได้**: ZORT ไม่เปิด API เซลเพจเลย (404 ครบ 221 คู่)
//       ⇒ ที่มาเดียวคือ "คนเปิดจอดูแล้วเห็น 0" ซึ่งเป็นภาพนิ่งของวันนั้น
//    ⚠️ ตระกูลเดียวกับที่เพิ่งเกือบทำเอกสารภาษี 694 ใบหายที่จอเอกสารบัญชี — เลข 0 ที่ลอกมา
//       จากภาพจอ ถูกอ่านเป็น "ตรวจแล้วไม่มี" ทั้งที่แปลว่า "วันนั้นจอโชว์ 0" เท่านั้น
//    ⇒ ความเสี่ยงของจอนี้ต่ำกว่ามาก (เซลเพจไม่ใช่เอกสารที่ต้องเก็บก่อนปิดบัญชี)
//       แต่ **ถ้อยคำต้องซื่อเท่ากัน** ไม่งั้นคนอ่านจะเรียนรู้ว่าเลขบนจอนี้เชื่อได้เสมอ
import LedgerScreen from '@/components/zort/LedgerScreen'

export default function SalePagesPage() {
  return (
    <LedgerScreen
      title="Sale pages"
      /* 🔬 `/SalePage/List` ของ ZORT: ตารางมีหัวคอลัมน์ ชื่อ · สินค้า · คลังสินค้า/สาขา แต่ **0 แถว**
         และจอนั้น **ไม่มีตัวนับจำนวนรายการ** ⇒ นับจาก API ก็ไม่ได้ นับจากจอก็ได้แค่ "ว่าง" */
      sumSeenAt="2026-09-16"
      /* ยืนยันสดด้วยตาเมื่อ 16 ก.ย. 2569 (อ่านอย่างเดียว ไม่กดปุ่ม) */
      sumLabel="จอ ZORT โชว์ 0 หน้า (ตารางว่าง 0 แถว · ZORT ไม่เปิด API ให้นับเอง)"
      cols={[
        { label: 'ชื่อ' },
        { label: 'สินค้า' },
        { label: 'คลังสินค้า/สาขา' },
      ]}
      createLabel="เพิ่มเซลเพจ"
      soonKey="salepage"
      purpose="หน้าขายสินค้าเดี่ยวสำหรับยิงโฆษณา — ลูกค้ากดสั่งจากหน้านั้นได้เลยโดยไม่ต้องเข้าเว็บร้าน"
      meanwhile="ร้านไม่ได้ใช้ของ ZORT เลย — gucut.com ทำหน้าที่นี้อยู่แล้วทั้งเว็บ"
    />
  )
}
