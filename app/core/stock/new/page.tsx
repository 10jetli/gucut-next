'use client'
// สินค้า → เพิ่มสินค้า (เขียนทะลุไป ZORT) — ปิดแถว 17
//
// ✅ **ปุ่มส่งจริงเปิดแล้ว 18 ก.ย. 2569** (ท่านประธานอนุมัติ · ใบ t_mu6i5c5k)
//    ข้างล่างนี้คือเหตุผลที่**เคยปิด** และสิ่งที่ทำให้เปิดได้ — เก็บไว้ทั้งดุ้น ห้ามลบ
//    เพราะคนรอบหน้าต้องรู้ว่าเราเคยกลัวอะไร และอะไรที่ทำให้เลิกกลัว
//
// 🔴 **เหตุผลเดิมที่ปิด (6 ก.ย. 2569)**
//    **ยังไม่รู้ว่า ZORT ของร้านตั้ง "ดันสินค้าใหม่ขึ้นมาร์เก็ตเพลสอัตโนมัติ" ไว้หรือเปล่า**
//    🔎 **ตรวจซ้ำ 18 ก.ย. 2569 (งานยืนไล่คำเตือนหมดอายุ) — ยังจริงอยู่ ยังไม่พบสวิตช์นี้**
//       ไล่เมนูตั้งค่าของ ZORT ทั้งหมด (`/Setting/*`) และหน้าเชื่อมต่อ (`/Integration/Main`, `/Integration/list`)
//       เจอแต่ **"การกระจายสินค้า" รายช่องทาง** (TIKTOK · Shopee-gucut · Lazada-gucut · Shopify = 100% ทุกช่อง)
//       กับสวิตช์ etax/สร้างคลังใหม่ ⇒ **ไม่เจอสวิตช์ดันสินค้าใหม่ขึ้นมาร์เก็ตเพลส**
//       ⚠️ "ไม่เจอ" ≠ "ไม่มี" — มันอาจอยู่ในหน้าตั้งค่าของแต่ละช่องทางซึ่งต้องกดเข้าไปทีละเจ้า
//       ⇒ **ยังต้องให้ท่านประธานยืนยันก่อนเปิดปุ่มส่งจริง เหมือนเดิม**
//    🔬 **ตรวจเพิ่ม 18 ก.ย. 2569 — ความเสี่ยงแคบลงมาก (อ่านจอ ZORT อย่างเดียว ไม่ได้กดบันทึก)**
//       ① ฟอร์ม `/Product/CreateMainProduct` ของ ZORT **ไม่มีช่อง/ช่องติ๊กเรื่องดันขึ้นช่องทางขายเลย**
//          (ช่องติ๊กที่มีคือ ดรอปชิป · Serial Number · ล็อต/วันหมดอายุ · สินค้าแถม)
//       ② การดันขึ้นช่องทางเป็น **คำสั่งแยกต่างหากของผู้ใช้** — ปุ่ม "เพิ่มสินค้าไปช่องทางการขายอื่น"
//          ในจอรายการสินค้า เปิดกล่องให้เลือกช่องทางแล้วกดบันทึก (`doOpenCreateProductData`)
//       ③ **กล่องนั้นมีช่องทางให้เลือกแค่ `Shopify` ช่องเดียว** — Shopee/Lazada/TikTok ไม่ได้อยู่ในรายการด้วยซ้ำ
//       ⇒ สินค้าที่สร้างจากจอนี้ **ไม่น่าโผล่หน้าร้านมาร์เก็ตเพลสเอง** · ความเสี่ยงที่เหลือคือ Shopify และยังเป็นคำสั่งมือ
//       ④ **เปิดดูหน้าตั้งค่าของแต่ละช่องทางครบแล้ว 18 ก.ย. 2569** (อ่านอย่างเดียว)
//          ไล่ครบทั้ง 7 ช่องทางที่เชื่อมไว้: TIKTOK · Shopee-gucut · Lazada-gucut · Shopify
//          · เพจเลื่อยยนต์ · @gucut1 · PEAK
//          ⇒ **ไม่มีช่องติ๊กเรื่องดัน/สร้างสินค้าอัตโนมัติเลยสักเจ้า**
//            (ที่มีคือ ยืนยันพร้อมส่งอัตโนมัติ · วันที่ยกเว้น · หน่วงเวลา · เวลาเข้ารับพัสดุ)
//          ⚠️ **"ไม่พบ" ยังไม่เท่ากับ "ไม่มี"** — สวิตช์อาจอยู่ในรูปอื่นที่ไม่ใช่ช่องติ๊ก
//             หรืออยู่ฝั่ง Seller Center ของมาร์เก็ตเพลสเอง ซึ่งไม่ได้อยู่ในมือเรา
//          ⇒ ความเสี่ยงแคบลงจนเหลือเท่าที่ "การอ่าน" พิสูจน์ได้ · ที่เหลือเป็นการตัดสินใจของท่านประธาน
//    ⇒ ถ้าตั้งไว้ สินค้าที่สร้างจากจอนี้ **จะโผล่หน้าร้านลูกค้าจริงทันที**
//       (Shopee/Lazada/TikTok) โดยไม่มีใครกดอะไรเพิ่ม
//
// ✅ **สิ่งที่ปิดช่องว่างนั้นได้จริง — สร้างของจริงหนึ่งตัวแล้วไปดูผล** (ฝั่ง CTO · 18 ก.ย. 11:50 น.)
//    TEST-20260918 (ไอดี 24171588 · ราคา 999,999 · สต็อก 0) สร้างผ่านเส้นท่อจริง
//    ⇒ จอ ZORT เห็นทันที และ **คอลัมน์ Marketplace ไม่มีไอคอนเลยสักช่องทาง**
//      (ต่างจากสินค้า 01209 ที่มีไอคอน Shopify) ⇒ **ไม่มีการดันขึ้นช่องทางอัตโนมัติ**
//    🔑 บทเรียนของใบนี้: การไล่อ่านตั้งค่าทำให้ความเสี่ยง**แคบลง**ได้ แต่ **ปิดไม่ได้**
//       เพราะทั้งหมดนั้นคือ "ไม่พบ" ซึ่งไม่เท่ากับ "ไม่มี" — ที่ปิดได้คือการทดลองที่ถอยกลับได้
//
// 🔴 **ของแถมจากการทดลองนั้น ซึ่งอันตรายกว่าตัวปุ่ม**
//    คนกดเพิ่มจากจอนี้ **จะไม่เห็นของที่ตัวเองเพิ่งสร้าง** ในจอสินค้า แม้กดรีเฟรช
//    ⇒ เชื่อว่าไม่สำเร็จ ⇒ กดเพิ่มซ้ำ ⇒ **สินค้าซ้ำใน ZORT ซึ่งลบผ่าน API ไม่ได้**
//
//    ⚠️ **สาเหตุที่แท้จริง — และร่างแรกของไฟล์นี้เขียนผิด (แก้ 18 ก.ย. 12:4x)**
//    ร่างแรกเขียนว่า "ZORT ไม่ส่งสินค้าใหม่มาในรายการรวมของ API" ⇒ **ไม่จริง**
//    ของจริง: จอสินค้าอ่านจาก **ภาพถ่ายสต็อกของวัน** (stock_snapshots) ไม่ได้อ่านทะเบียนสินค้า
//    ภาพของวันถูกถ่าย**ก่อน**สินค้าถูกสร้าง ⇒ ของใหม่ไม่อยู่ในภาพนั้น
//    สั่งถ่ายภาพรอบใหม่แล้วเห็นทันที (ฝั่ง CTO พิสูจน์: ก่อนถ่าย ค้นไม่เจอ · ถ่ายแล้วได้ 2,674 รหัส เจอทันที)
//    🔑 **เหตุผลที่ผิดอันตรายกว่าไม่มีเหตุผล**: ถ้าจอบอกว่า "ZORT ยังไม่ส่ง" คนจะไปนั่งรอ ZORT
//       ซึ่งไม่มีวันเปลี่ยน — ของที่ต้องทำจริงคือกดถ่ายภาพรอบใหม่ (จอนี้จึงมีปุ่มนั้นให้เลย)
//
// 🚫 **ยังไม่เปิด และห้ามเปิดพร้อมกัน**: ลบสินค้าเป็นชุด · แก้ไขเป็นชุด (รอท่านประธานสั่งแยก)
// 🚫 **ห้ามลบ TEST-20260918 ก่อน 20 ก.ย. 2569** (ต้องดูข้ามวันว่ามันเข้ากระจกเมื่อไหร่)
import { useCallback, useEffect, useMemo, useState } from 'react'
import Link from 'next/link'
import { PageHead, WriteResult } from '@/components/zort'
import type { WriteResp } from '@/components/zort'
import { ส่งจริงได้ } from '@/lib/real-send'

/** ✅ **เปิดแล้ว 18 ก.ย. 2569 — ท่านประธานอนุมัติเอง** (ใบกระดาน t_mu6i5c5k)
 *
 *  สองข้อที่เคยกั้นไว้ ตอบได้ครบทั้งคู่ด้วย **การทดลองจริง ไม่ใช่การอ่านอย่างเดียว**:
 *  ① ท่านประธานอนุมัติจอนี้โดยเฉพาะ (12:0x น.)
 *  ② ฝั่ง CTO สร้างสินค้าทดสอบผ่านเส้นท่อจริงเมื่อ 11:50 น. (TEST-20260918 · ไอดี 24171588
 *     ราคา 999,999 สต็อก 0) แล้ว **เปิดจอ ZORT ดูคอลัมน์ Marketplace: ไม่มีไอคอนเลยสักช่องทาง**
 *     ต่างจากสินค้า 01209 ที่มีไอคอน Shopify ⇒ **ไม่มีการดันขึ้นช่องทางอัตโนมัติ**
 *  ⚠️ ของเดิมเรามีแค่ "ไล่หน้าตั้งค่าแล้วไม่เจอสวิตช์" ซึ่งคือ "ไม่พบ" ไม่ใช่ "ไม่มี"
 *     ⇒ ที่ปิดช่องว่างนั้นได้จริงคือการสร้างของจริงหนึ่งตัวแล้วไปดูผล
 *
 *  🚫 **ที่ยังไม่เปิดและห้ามเปิดพร้อมกัน**: ลบสินค้าเป็นชุด · แก้ไขเป็นชุด (รอท่านประธานสั่งแยก)
 *  🚫 **ห้ามลบ TEST-20260918 ก่อน 20 ก.ย. 2569** — ฝั่ง CTO ต้องดูข้ามวันว่ามันโผล่ในรายการรวมเมื่อไหร่ */
/* ⚠️ ค่าอยู่ที่ `lib/real-send.ts` ที่เดียว — **ห้ามเขียนค่าตายตรงนี้**
   เพราะค่านี้คือ *สถานะการอนุมัติของท่านประธาน* ไม่ใช่ค่าคงที่ของโค้ด
   (เขียนซ้ำหลายที่มาแล้ว 12 ไฟล์ ⇒ จอรายการซื้อพูดเท็จอยู่ 5 วัน · ใบ S4 19 ก.ย. 2569) */
const REAL_SEND_ENABLED = ส่งจริงได้('stock/new')
/* 🔴 **ทำไมกล่องข้อความหลังส่งสำเร็จถึงเป็นส่วนหนึ่งของความถูกต้อง ไม่ใช่ของประดับ**
   คนกดเพิ่มจะไม่เห็นของที่เพิ่งสร้างในจอสินค้า (จอนั้นอ่านจากภาพถ่ายสต็อกของวัน)
   ⇒ ถ้าจอเงียบ คนจะสรุปว่าไม่สำเร็จแล้วกดซ้ำ ⇒ สินค้าซ้ำใน ZORT ที่ลบผ่าน API ไม่ได้
   ⇒ นี่คือ "ระบบทำงานถูก แต่สื่อสารผิด" ตัวจริง — และคำอธิบายที่ผิดก็พาคนไปรอผิดที่เหมือนกัน
      (ดูย่อหน้า "สาเหตุที่แท้จริง" บนหัวไฟล์ ว่าร่างแรกเขียนผิดไว้ว่าอย่างไร) */

/** ช่องกรอกหนึ่งช่อง — **ต้องอยู่ระดับโมดูล** (เหตุผลอยู่ในคอมเมนต์ที่จุดเรียก) */
function Field({ label, value, onChange, ph, hint }:
  { label: string; value: string; onChange: (e: React.ChangeEvent<HTMLInputElement>) => void; ph?: string; hint?: string }) {
  return (
    <label className="block">
      <span className="text-[12.5px] text-gray-600">{label}</span>
      <input value={value} onChange={onChange} placeholder={ph}
        className="w-full border border-gray-300 rounded px-3 py-2 text-[14px] mt-1" />
      {hint && <span className="block text-[11.5px] text-gray-400 mt-0.5">{hint}</span>}
    </label>
  )
}

export default function NewProductPage() {
  /* 🔑 **ช่องขนส่งกับ Tag เพิ่ม 18 ก.ย. 2569** — ท่อ `?addproduct=1` รับ `weight/width/length/height/tags`
     มาตั้งแต่ต้น (ยืนยันในซอร์สท่อ `zortAddProduct` + เอกสาร ZORT API V4)
     แต่จอนี้เขียนไว้เองว่า "ท่อเรายังไม่มีช่องนี้" ⇒ **คำกล่าวอ้างที่เป็นเท็จโดยไม่มีใครรู้**
     ⇒ ตรงกับกติกาที่ตกลงกันคืนนี้: **ก่อนบอกว่าอีกฝั่งไม่มี ให้ไปอ่านของอีกฝั่งก่อน** */
  const [f, setF] = useState({ sku: '', name: '', price: '', cost: '', unit: '', barcode: '', category: '',
    weight: '', width: '', length: '', height: '', tags: '' })
  const [busy, setBusy] = useState(false)
  const [err, setErr] = useState('')
  const [res, setRes] = useState<WriteResp | null>(null)
  const [okDry, setOkDry] = useState('')
  /** ส่งจริงไปแล้วในหน้านี้ ⇒ ปิดปุ่มถาวรจนกว่าจะเปิดหน้าใหม่ (ดูเหตุผลที่ setLocked) */
  const [locked, setLocked] = useState(false)
  /** ผลการสั่งถ่ายภาพสต็อกรอบใหม่ — ว่าง = ยังไม่ได้สั่ง */
  const [snap, setSnap] = useState('')
  /* 🔴 **สร้างเลขอ้างอิงฝั่งเบราว์เซอร์เท่านั้น** (แก้ 14 ก.ย. 2569 · เจอตอนกวาดทุกหน้าด้วยเบราว์เซอร์จริง)
     `useState(() => …Math.random()…)` ทำงาน **ทั้งตอน SSR และตอน hydrate** ⇒ ได้คนละค่า
     React จึงฟ้อง "Text content did not match" แล้ว **ทิ้ง HTML ฝั่งเซิร์ฟเวอร์ทั้งหน้า**
     ⇒ ไม่ได้ทำให้เลขซ้ำ (ค่าฝั่งเบราว์เซอร์ชนะเสมอ กลไกกันส่งซ้ำจึงยังดี)
        แต่เป็น error จริงในคอนโซล ซึ่งกลบ error อื่นที่ควรเห็น และทำให้ทั้งหน้าเรนเดอร์ใหม่
     ⚠️ **ห้ามเปลี่ยนกลไกกันส่งซ้ำ** — ยังต้องสร้างครั้งเดียวต่อการเปิดหน้าหนึ่งครั้ง
        (deps ว่าง) ไม่ใช่สร้างใหม่ทุกครั้งที่กด ไม่งั้นกดสองครั้ง = ได้เอกสารสองใบใน ZORT ที่ลบไม่ได้ */
  const [ref, setRef] = useState('')
  useEffect(() => {
    setRef(`PD-${new Date().toISOString().slice(0, 10).replace(/-/g, '')}-${Math.random().toString(36).slice(2, 7)}`)
  }, [])

  const set = (k: keyof typeof f) => (e: React.ChangeEvent<HTMLInputElement>) => setF((v) => ({ ...v, [k]: e.target.value }))
  const sig = useMemo(() => JSON.stringify(f), [f])
  const dryOk = okDry !== '' && okDry === sig

  const send = useCallback(async (confirm: boolean) => {
    setErr(''); setRes(null)
    if (!f.sku.trim() || !f.name.trim()) { setErr('ต้องมีทั้งรหัสสินค้าและชื่อสินค้า'); return }
    /* ⚠️ ตัวเลขต้องเป็นตัวเลขจริง — ท่อจะตีกลับอยู่แล้ว แต่บอกที่จอเร็วกว่าและชัดกว่า */
    for (const [k, label] of [['price', 'ราคาขาย'], ['cost', 'ราคาซื้อ'],
      ['weight', 'น้ำหนัก'], ['width', 'กว้าง'], ['length', 'ยาว'], ['height', 'สูง']] as const) {
      const raw = f[k].trim()
      if (raw !== '' && !Number.isFinite(Number(raw))) { setErr(`${label}ต้องเป็นตัวเลข`); return }
    }
    setBusy(true)
    try {
      const r: WriteResp = await fetch('/api/web/core?addproduct=1', {
        method: 'POST', headers: { 'content-type': 'application/json' },
        body: JSON.stringify({
          ref,
          sku: f.sku.trim(),
          name: f.name.trim(),
          ...(f.price.trim() ? { price: Number(f.price) } : {}),
          ...(f.cost.trim() ? { cost: Number(f.cost) } : {}),
          ...(f.unit.trim() ? { unit: f.unit.trim() } : {}),
          ...(f.barcode.trim() ? { barcode: f.barcode.trim() } : {}),
          ...(f.category.trim() ? { category: f.category.trim() } : {}),
          /* ⚠️ ท่อตีกลับถ้าไม่ใช่ตัวเลขไม่ติดลบ · ช่องว่างไม่ส่ง (ท่อข้ามให้เอง) */
          ...(f.weight.trim() ? { weight: Number(f.weight) } : {}),
          ...(f.width.trim() ? { width: Number(f.width) } : {}),
          ...(f.length.trim() ? { length: Number(f.length) } : {}),
          ...(f.height.trim() ? { height: Number(f.height) } : {}),
          /* Tag: ท่อรับเป็นอาเรย์ (≤20 ตัว ตัวละ ≤40 ตัวอักษร) ⇒ จอรับเป็นคำคั่นด้วยจุลภาค */
          ...(f.tags.trim() ? { tags: f.tags.split(',').map((t) => t.trim()).filter(Boolean) } : {}),
          ...(confirm ? { confirm: true } : {}),
        }),
      }).then((x) => x.json())
      setRes(r)
      if (!confirm && r?.dryRun && r?.ok !== false) setOkDry(sig)
      /* 🔴 **ล็อกใบนี้ทันทีที่ปลายทางรับไปแล้ว — รวมกรณี "เคยบันทึกแล้ว" และ "ไม่รู้ผล"**
         ทั้งสามกรณีมีของอยู่ใน ZORT แล้ว (หรืออาจมี) ⇒ กดซ้ำได้ = ทางไปสู่สินค้าซ้ำ
         และ ZORT ลบสินค้าผ่าน API ไม่ได้ ⇒ ปิดทางไว้ก่อน ให้เปิดหน้าใหม่ถ้าจะเพิ่มตัวถัดไป */
      if (confirm && (r?.ok || r?.duplicate || r?.unknown)) { setOkDry(''); setLocked(true) }
    } catch (e) { setErr(String(e instanceof Error ? e.message : e)) } finally { setBusy(false) }
  }, [f, ref, sig])

  /* 🔴🔴 **เคยเป็นบั๊กที่ทำให้ฟอร์มนี้ใช้ไม่ได้เลย — พิมพ์ได้ทีละตัวอักษร** (แก้ 18 ก.ย. 2569)
     เดิมประกาศ `const F = (...) => ...` **ไว้ในตัว component** ⇒ ทุกครั้งที่ state เปลี่ยน
     React เห็นเป็น **ชนิด component ตัวใหม่** ⇒ **ถอด `<input>` เดิมทิ้งแล้วสร้างใหม่**
     ⇒ พิมพ์ตัวแรก → state เปลี่ยน → input ถูกสร้างใหม่ → **โฟกัสหลุดไปที่ body**
     ⇒ ตัวที่สองเป็นต้นไปไม่เข้าช่องเลย · ค่าที่ส่งท่อจึงเหลือ **ตัวอักษรเดียวทุกช่อง**
     🔬 วัดของจริง: พิมพ์ `ABCDE` ทีละตัว ⇒ ค่าในช่อง = `'A'` · `document.activeElement` = `BODY`
        และโหมดซ้อมส่งท่อได้ `{sku:'Z', name:'ท', weight:'2', tag:['โ']}`
     🔑 **ทำไมไม่มีใครเจอมาก่อน**: จอเรนเดอร์ครบ ไม่มี error ไม่มีอะไรแดง ตัวกวาดจอผ่านฉลุย
        และคนที่ "สร้างสินค้าจริงสำเร็จ" ทำผ่าน **เส้นท่อโดยตรง ไม่ได้พิมพ์บนฟอร์มนี้**
        ⇒ ของที่ไม่เคยมีใคร **ใช้จริงด้วยมือ** จะดูดีตลอดกาล
     ⚠️ **ห้ามย้าย F กลับเข้าไปข้างในอีก** ไม่ว่าจะเพื่อให้อ่านง่ายแค่ไหน */

  return (
    <div className="p-4 md:p-6 max-w-[820px]">
      <PageHead
        title="เพิ่มสินค้า"
        summary={<>สร้างสินค้าใน ZORT โดยตรง{' | '}<span className="text-gray-400">เลขอ้างอิง: <b>{ref}</b> (กันการส่งซ้ำ)</span></>}
        actions={<Link href="/core/stock" className="text-[13px] text-blue-600 hover:underline">← กลับรายการสินค้า</Link>}
      />

      {/* 🔴 คำเตือนที่ต่างจากจออื่น — จอนี้มีทางที่ของหลุดออกไปถึงลูกค้า */}
      {/* 🔴 กล่องนี้เคยเขียนว่า "ตอนนี้ยังกดส่งจริงไม่ได้" — **ต้องเปลี่ยนพร้อมกับการเปิดปุ่ม**
          จอที่บอกว่ากดไม่ได้ทั้งที่กดได้ คือบั๊ก ไม่ใช่เรื่องถ้อยคำ (กฎข้อ 3 ใน CLAUDE.md) */}
      <div className="text-[12.5px] text-amber-900 bg-amber-50 border border-amber-300 rounded-md px-3.5 py-2.5 mb-4 leading-relaxed">
        ⚠️ <b>จอนี้เขียนทะลุไป ZORT จริง และ ZORT ลบสินค้าผ่าน API ไม่ได้</b> — ส่งแล้วส่งเลย
        <br />
        ✅ <b>เปิดให้ส่งจริงแล้ว 18 ก.ย. 2569</b> (ท่านประธานอนุมัติ) หลังทดลองสร้างของจริงหนึ่งตัว
        แล้วเปิดจอ ZORT ดู: คอลัมน์ Marketplace <b>ไม่มีไอคอนเลยสักช่องทาง</b>
        {' '}⇒ <b>ไม่มีการดันขึ้นมาร์เก็ตเพลสอัตโนมัติ</b> สินค้าที่สร้างจากจอนี้จะไม่โผล่หน้าร้านลูกค้าเอง
        <br />
        📦 <b>ของที่เพิ่งสร้างจะขึ้นในจอสินค้าหลังภาพถ่ายสต็อกรอบถัดไป</b> —
        {' '}<b>ไม่ใช่ว่าสร้างไม่สำเร็จ</b> · อย่ากดเพิ่มซ้ำเพราะหาไม่เจอในรายการ (มีปุ่มสั่งถ่ายภาพให้หลังส่งสำเร็จ)
      </div>

      <div className="bg-white border border-gray-200 rounded-md p-4 grid grid-cols-1 sm:grid-cols-2 gap-3">
        {/* 🔤 **ป้ายช่องลอกจากจอ ZORT จอเดียวกัน** (`/Product/CreateMainProduct` · อ่านอย่างเดียว 16 ก.ย. 2569)
            เดิมเราตั้งชื่อเอง: "ต้นทุน" · "หน่วยนับ" · "บาร์โค้ด" ⇒ คนที่ชิน ZORT ต้องแปลในหัวทุกช่อง
            ZORT เรียก: ราคาซื้อ · หน่วย · รหัสคิวอาร์โค้ดและบาร์โค้ด (คำสั่งท่านประธาน: ให้เหมือน 100%) */}
        <Field label="รหัสสินค้า *" value={f.sku} onChange={set('sku')} ph="เช่น 03731" hint="ใช้เป็นกุญแจของทั้งระบบ — ตั้งผิดแล้วแก้ยาก (ZORT เรียกช่องนี้ว่ารหัสสินค้า ไม่ใช่ SKU)" />
        <Field label="ชื่อสินค้า *" value={f.name} onChange={set('name')} ph="ชื่อที่จะโชว์" />
        <Field label="ราคาขาย" value={f.price} onChange={set('price')} ph="ไม่ใส่ก็ได้" />
        <Field label="ราคาซื้อ" value={f.cost} onChange={set('cost')} ph="ไม่ใส่ก็ได้" />
        <Field label="หน่วย" value={f.unit} onChange={set('unit')} ph="ตัวอย่าง: ชิ้น, ตัว" />
        <Field label="รหัสคิวอาร์โค้ดและบาร์โค้ด" value={f.barcode} onChange={set('barcode')} ph="ไม่ใส่ก็ได้" />
        <Field label="น้ำหนัก (กรัม)" value={f.weight} onChange={set('weight')} ph="ไม่ใส่ก็ได้" hint="กล่อง “ข้อมูลขนส่ง” ของ ZORT — หน่วยตามที่จอ ZORT เขียนไว้" />
        <Field label="กว้าง (ซม.)" value={f.width} onChange={set('width')} ph="ไม่ใส่ก็ได้" />
        <Field label="ยาว (ซม.)" value={f.length} onChange={set('length')} ph="ไม่ใส่ก็ได้" />
        <Field label="สูง (ซม.)" value={f.height} onChange={set('height')} ph="ไม่ใส่ก็ได้" />
        <Field label="Tag สินค้า" value={f.tags} onChange={set('tags')} ph="คั่นด้วยจุลภาค เช่น โซ่, NEWWAVE" hint="ท่อรับได้ไม่เกิน 20 แท็ก แท็กละ 40 ตัวอักษร" />
        <Field label="หมวดหมู่" value={f.category} onChange={set('category')} ph="ไม่มีหมวดหมู่" hint="ต้องเป็นชื่อที่มีอยู่แล้วใน ZORT ไม่งั้นมันจะเมินช่องนี้เงียบ ๆ (จอ ZORT สร้างหมวดใหม่ได้ในจอเดียวกัน จอเรายังไม่ได้)" />
      </div>

      {/* 🔬 **เทียบกับจอ ZORT จริงแล้วเขียนไว้บนจอ** — ใบงานยืน t_mu2u9mym
          อ่านโครงฟอร์ม `/Product/CreateMainProduct` แบบอ่านอย่างเดียว 16 ก.ย. 2569
          (เก็บไว้ที่ `~/zort-export-private/zort-เพิ่มสินค้า-โครงจอ.json` ไม่เข้า repo)
          ZORT แบ่งเป็น 6 กล่อง: รายละเอียดสินค้า · ราคาสินค้า · ข้อมูลขนส่ง · ตั้งค่า · สินค้าหลากคุณสมบัติ · คลังสินค้า
          ⚠️ **ห้ามแปะช่องพวกนี้ไปก่อนแล้วเงียบ** — ยังไม่ได้ยิงตรวจว่าท่อ addproduct รับช่องไหนได้
             และ ZORT **เมินช่องที่ไม่รู้จักแบบไม่บอก** ⇒ เขียนไว้ว่ายังไม่มี ตรงกว่าใส่ช่องที่ส่งไปแล้วหาย */}
      <div className="text-[12px] text-gray-600 bg-gray-50 border border-gray-200 rounded-md px-3.5 py-2.5 mt-3 leading-relaxed">
        <b>ช่องที่จอ ZORT มี แต่จอนี้ยังไม่มี</b> (เทียบของจริง 16 ก.ย. 2569)
        <ul className="list-disc ml-5 mt-1 space-y-0.5">
          <li><b>ประเภท: สินค้า / บริการ</b> — ของเราสร้างได้แบบเดียว ยังไม่รู้ว่าท่อส่งประเภทได้ไหม</li>
          <li><b>รูปสินค้า (ZORT ใส่ได้ 9 รูปตอนสร้าง)</b> — จอเราใส่รูปได้ทีหลังทีละรหัสที่จอแก้สินค้า</li>
          {/* ✅ **สองข้อนี้ถูกถอดออก 18 ก.ย. 2569 — เพราะมันเป็นเท็จมาตลอด**
              เดิมเขียนว่า "ท่อเรายังไม่มีช่องนี้" แต่ `zortAddProduct` ของท่อรับ weight/width/length/height/tags
              มาตั้งแต่ต้น (ยืนยันจากซอร์สท่อ + เอกสาร ZORT API V4) ⇒ **จอเขียนดูถูกความสามารถของท่อเอง**
              🔑 คลาสเดียวกับ channel-gaps กลับด้าน: ท่อมีของ แต่จออ้างว่าไม่มี ⇒ ไม่มีใครไปใช้
                 และคำอ้างแบบนี้ **ไม่มีด่านไหนจับได้** เพราะมันพูดถึงของอีก repo หนึ่ง */}
          <li><b>Tag สินค้า — ตอนค้นหา</b> ยังไม่มีในจอสินค้า (ตอนสร้างมีแล้ว)</li>
          <li><b>Serial Number · ล็อต/วันหมดอายุ · สินค้าแถม · แสดงในหน้าสั่งซื้อ (ดรอปชิป)</b> — กล่อง &ldquo;ตั้งค่า&rdquo; ของ ZORT ทั้งกล่อง</li>
          <li><b>ยอดยกมา + สินค้าเข้าที่คลังไหน</b> — ZORT ตั้งจำนวนตั้งต้นให้ได้ตอนสร้าง · ของเราสร้างแล้วยอดเป็น 0 ต้องไปตั้งต่างหาก</li>
          <li><b>สินค้าหลากคุณสมบัติ (สี/ขนาด)</b> — ZORT ผูกได้ตอนสร้าง (จอ <code>/Product/Variantlist</code> ของร้านมี 0 รายการ ⇒ ร้านยังไม่ได้ใช้)</li>
          <li><b>สร้างหมวดหมู่ใหม่ในจอเดียวกัน</b> — ของเราต้องมีหมวดอยู่ก่อน</li>
        </ul>
        <div className="mt-1.5">
          🖼️ ZORT ยังมีจอแยกชื่อ <b>จัดการรูปภาพสินค้า</b> (<code>/Product/AddPicturelist</code>) —
          อัปโหลดรูปหลายรหัสรวดเดียว กรองด้วยรหัส/ชื่อ/ช่วงราคา/หมวดหมู่/คลัง/Tag ·
          หัวจอนับ <b>2,898 รายการ</b> — <b>ตอบได้แล้ว 18 ก.ย. 2569 ว่าต่างกันเพราะอะไร</b>:
          {' '}ZORT นับ<b>สินค้าที่ไม่มีรหัสด้วย</b> ส่วนทะเบียนเราเก็บเฉพาะที่มีรหัส
          {' '}(วัดรุ่นเดียวกัน: ZORT 2,899 · เรา 2,673 · หักที่ไม่มีรหัส 226 ตัว <b>เหลือเศษ 0</b>) ·
          <b>จอเรายังไม่มีจอแบบนั้น</b> ⇒ ตอนนี้รูปที่ยังไม่มีต้องไปใส่ที่ ZORT
        </div>
      </div>

      {err && <div className="text-[13px] text-red-800 bg-red-50 border border-red-300 rounded-md px-3.5 py-2.5 mt-3">{err}</div>}
      <WriteResult r={res} />

      {/* 📦 **ข้อความหลังส่งสำเร็จ — เป็นส่วนหนึ่งของความถูกต้อง ไม่ใช่คำอธิบายประกอบ**
          ถ้าไม่มีกล่องนี้ คนจะกลับไปจอสินค้า ไม่เห็นของที่เพิ่งสร้าง แล้วกดสร้างซ้ำ */}
      {locked && res && !res.dryRun && (res.ok || res.duplicate || res.unknown) && (
        <div className="text-[13px] text-emerald-950 bg-emerald-50 border border-emerald-300 rounded-md px-3.5 py-3 mt-3 leading-relaxed">
          <b>📦 สินค้าที่เพิ่งสร้าง จะขึ้นในจอ &ldquo;สินค้า&rdquo; หลัง <u>ภาพถ่ายสต็อกรอบถัดไป</u> — ไม่ใช่ว่าสร้างไม่สำเร็จ</b>
          <br />
          จอสินค้าของเราอ่านจาก <b>ภาพถ่ายสต็อกของวัน</b> ไม่ได้อ่านทะเบียนสินค้าตรง ๆ
          {' '}⇒ ของที่สร้างหลังภาพถูกถ่าย จะยังไม่อยู่ในภาพนั้น
          <br />
          ⚠️ <b>ห้ามกดเพิ่มซ้ำเพราะไม่เห็นในรายการ</b> — จะได้สินค้าสองตัวใน ZORT และ
          {' '}<b>ZORT ลบสินค้าผ่าน API ไม่ได้</b>
          <br />
          ✅ กดปุ่มข้างล่างเพื่อ<b>ถ่ายภาพสต็อกรอบใหม่</b>แล้วจะเห็นทันที · หรือเปิด ZORT
          {' '}ค้นด้วยรหัส <code>{f.sku.trim()}</code> (เลขอ้างอิงใบนี้: <code>{ref}</code>)
          <div className="mt-2 flex flex-wrap items-center gap-2">
            <button
              onClick={async () => {
                setSnap('กำลังถ่ายภาพสต็อกรอบใหม่…')
                try {
                  const r = await fetch('/api/web/core?snapshot=1')
                  const d = await r.json()
                  if (!r.ok) throw new Error(`HTTP ${r.status}`)
                  /* ⚠️ บอกเลขที่ได้กลับมาจริง ๆ ไม่ใช่เขียนว่า "เสร็จแล้ว" ลอย ๆ
                     คนต้องเห็นว่าภาพรอบใหม่นับได้กี่รหัส ถึงจะรู้ว่าของตัวเองเข้าหรือยัง */
                  setSnap(`ถ่ายภาพรอบใหม่แล้ว${d?.day ? ` (วันที่ ${d.day}` : ''}${
                    typeof d?.skus === 'number' ? ` · ${d.skus.toLocaleString('th-TH')} รหัส` : ''}${d?.day ? ')' : ''}`
                    + ' — เปิดจอสินค้าแล้วค้นรหัสนี้ได้เลย')
                } catch (e) {
                  setSnap(`ถ่ายภาพไม่สำเร็จ: ${e instanceof Error ? e.message : String(e)} — ลองใหม่ได้ ของที่สร้างไปแล้วไม่หาย`)
                }
              }}
              disabled={snap === 'กำลังถ่ายภาพสต็อกรอบใหม่…'}
              className="text-[13px] font-semibold text-emerald-900 bg-white border border-emerald-400 rounded-full px-4 py-1.5 disabled:opacity-50">
              📸 ถ่ายภาพสต็อกเดี๋ยวนี้
            </button>
            {snap && <span className="text-[12px] text-emerald-900">{snap}</span>}
          </div>
        </div>
      )}

      <div className="flex flex-wrap items-center gap-3 mt-4">
        <button onClick={() => send(false)} disabled={busy || !ref || locked}
          className="text-[14px] font-semibold text-gray-800 bg-white border-2 border-gray-300 rounded-full px-6 py-2 disabled:opacity-50 hover:bg-gray-50">
          {busy ? 'กำลังส่ง…' : '🧪 ทดลองส่ง (ยังไม่เข้า ZORT)'}
        </button>
        {/* 🔴 ส่งจริงไปแล้ว ⇒ **ไม่มีปุ่มให้กดอีก** ไม่ใช่แค่ปุ่มจาง ๆ ที่ยังเล็งกดได้ */}
        {locked ? (
          <span className="text-[13px] font-semibold text-emerald-900 bg-emerald-100 border border-emerald-300 rounded-full px-5 py-2">
            ส่งใบนี้ไปแล้ว — ปิดการส่งซ้ำไว้
          </span>
        ) : (
          <button onClick={() => send(true)} disabled={busy || !ref || !dryOk || !REAL_SEND_ENABLED}
            className="text-[14px] font-semibold text-white rounded-full px-6 py-2 disabled:opacity-40"
            style={{ background: dryOk && REAL_SEND_ENABLED ? '#c0392b' : '#9aa0a6' }}>
            ส่งจริงเข้า ZORT
          </button>
        )}
        {locked
          ? (
            /* ⚠️ ต้อง **โหลดหน้าใหม่จริง ๆ** ไม่ใช่ลิงก์ไปหน้าเดิม
               ลิงก์ไปเส้นทางเดียวกันใน Next จะไม่เกิดใบใหม่ ⇒ เลขอ้างอิงเดิมค้าง
               ⇒ ปุ่มที่กดแล้วไม่มีอะไรเกิดขึ้น = ปุ่มหลอกอีกตัว */
            <button onClick={() => window.location.reload()} className="text-[12.5px] text-blue-600 hover:underline">
              เพิ่มสินค้าตัวถัดไป (เปิดใบใหม่ — จะได้เลขอ้างอิงใหม่)
            </button>
          )
          : !REAL_SEND_ENABLED
            ? <span className="text-[12.5px] text-amber-800"><b>ยังไม่เปิดให้ส่งจริง</b> — รอคำตอบเรื่องการดันขึ้นมาร์เก็ตเพลส</span>
            : !dryOk && <span className="text-[12.5px] text-gray-500">{okDry === '' ? 'ต้องกดทดลองส่งให้ผ่านก่อน' : 'เนื้อหาเปลี่ยนหลังทดลองส่ง — ต้องทดลองใหม่'}</span>}
      </div>

      <p className="text-[11.5px] text-gray-400 mt-3 leading-relaxed">
        ⚠️ <b>ZORT เมินช่องที่ไม่รู้จักแบบเงียบ ๆ</b> — ส่งชื่อหมวดที่ไม่มีอยู่จริง มันจะสร้างสินค้าให้
        โดยไม่ใส่หมวด และ<b>ไม่บอกว่าไม่ได้ใส่</b> ⇒ หลังส่งจริงต้องเปิดดูใน ZORT ว่าช่องที่ตั้งใจใส่เข้าครบไหม ·
        ระบบกันส่งซ้ำด้วยเลขอ้างอิง — กดสองครั้งจะไม่ได้สินค้าสองตัว
      </p>
    </div>
  )
}
