/* ด่านที่ไม่มีของให้ตรวจ ต้องไม่ขึ้นเขียว (18 ก.ย. 2569 · งาน S2 จากคลังงานสำรอง)
 *
 * 🔴 **ที่มา — วัดจริง ไม่ได้เดา**: รันด่านทั้ง 26 ตัวในสำเนาที่มีแต่โฟลเดอร์เปล่า
 *    **11 ตัวขึ้น ✅ ทั้งที่ไม่ได้อ่านไฟล์สักไฟล์**
 *    ⇒ เขียวของมันแปลว่า "ยังไม่เจอ" ไม่ใช่ "ไม่มี" — ซึ่งอ่านเหมือนกันทุกประการบนจอ build
 *
 * อาการที่จะเกิดจริงในอนาคต ไม่ใช่เรื่องสมมติ:
 *   ย้าย/เปลี่ยนชื่อโฟลเดอร์ (เช่น `app/core` → ที่อื่น) ⇒ ด่านกวาดไม่เจอไฟล์
 *   ⇒ build เขียวทั้งแถบ ทั้งที่ไม่มีใครตรวจอะไรเลย
 *   (ตระกูลเดียวกับ check-cron-table ที่ `\w` ไม่แมตช์ชื่อไฟล์ภาษาไทย แล้วเงียบมาทั้งวัน)
 *
 * ⚠️ **ตัวเลขพื้นต้องต่ำแบบปลอดภัย** — ไม่ใช่ล็อกจำนวนไฟล์ปัจจุบัน
 *    ล็อกไว้เท่าวันนี้ = ด่านจะแดงทุกครั้งที่ลบไฟล์ตามปกติ แล้วคนจะปิดด่านทิ้ง
 *    หน้าที่ของตัวนี้คือจับ "ศูนย์" กับ "เกือบศูนย์" ไม่ใช่เฝ้าจำนวน
 */
export function ต้องมีของให้ตรวจ(จำนวน, ชื่อด่าน, พื้น = 1) {
  if (typeof จำนวน === 'number' && จำนวน >= พื้น) return จำนวน
  console.error(`\n🔴 ${ชื่อด่าน}: ไม่มีไฟล์ให้ตรวจเลย (อ่านได้ ${จำนวน ?? 'ไม่รู้'} ไฟล์)`)
  console.error('   ⇒ ด่านนี้ตรวจอะไรไม่ได้รอบนี้ **ถือว่าไม่ผ่าน** — เขียวที่แปลว่า "ยังไม่เจอ" อันตรายกว่าไม่มีด่าน')
  console.error('   มักเกิดจาก: ย้าย/เปลี่ยนชื่อโฟลเดอร์ที่ด่านนี้กวาด หรือรันจากไดเรกทอรีผิด')
  process.exit(1)
}
