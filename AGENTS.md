# AGENTS.md — กติกาสำหรับ agent เสริม (Codex) ใน repo `gucut-next`

> repo นี้คือหลังร้านของร้านเลื่อยยนต์จริง (admin.gucut.com) — โค้ดที่ผิดทำให้คนขายของผิด
> เอกสารนี้คือ **No-list ที่ตายจริง** ไม่ใช่คู่มือฉบับย่อ · ทุกข้อแลกมาด้วยของพังจริง
> สงสัยข้อไหน = **ถามผ่านเจ้าของร้าน อย่าเดา** (การเดาคือที่มาของทุกข้อในไฟล์นี้)

## ขอบเขตการทำงาน

- ทำงานใน **git worktree แยก** บน branch `codex/<งาน>` เท่านั้น
  🔴 ห้าม checkout/สลับ branch ในโฟลเดอร์หลัก (`~/gucut-next`) — มี agent อื่นทำงานค้างอยู่
- 🔴 **ห้าม push `main`** — งานส่งเป็น branch ให้เจ้าของ repo รีวิวก่อน merge เสมอ
- 🔴 **ไม่มีคีย์/รหัสใด ๆ ให้ใช้** — งานที่ต้องยิง API จริง เขียนสคริปต์ไว้ให้คนรีวิวยิงแทน
  ห้ามอ่าน/คัดลอกไฟล์รหัส (`~/.gucut-admin-key` ฯลฯ) และห้าม hardcode รหัสลงโค้ด
- branch อายุ **ไม่เกินวันเดียว** — main ขยับทุกวัน ค้างข้ามวันให้ปิดทิ้งแล้วเริ่มใหม่
- รายงาน/ผลรีวิว = ไฟล์ `.md` ใน branch ของตัวเอง · **ห้ามเขียนไฟล์ใน `~/claude-shared`**

## 🔴 ไฟล์/เรื่องที่ห้ามแตะเด็ดขาด (แตะแล้วความเสียหายถาวรหรือเป็นเรื่องกฎหมาย)

| ห้าม | เพราะ |
|---|---|
| ทุกอย่างใน repo `gucut-web` | คนละเขต — repo นั้นมีเจ้าของและกติกาของตัวเอง |
| โค้ด/ค่าที่เกี่ยว **webhook LINE @gucut1** | แชท ZORT ของร้านใช้อยู่ทุกวัน แตะแล้วพังทั้งระบบแชท |
| การ **Reissue token** ใด ๆ ของ LINE | token เก่าตายทันที ระบบที่ใช้ร่วมพังหมด |
| เขียนอะไรลง **ZORT / D1 / Blobs จริง** | เอกสารใน ZORT **ลบผ่าน API ไม่ได้** — สร้างผิดค้างถาวร |
| `scripts/.claims-count` (แก้ค่าเอง) | มันคือหมุดกันตัวตรวจตาบอด — เลขลดต้องมาจากคนอธิบายได้ |

## 🔴 กฎเนื้อหาที่ห้ามละเมิด (มีผลทางกฎหมาย/ธุรกิจจริง)

1. **ห้ามพิมพ์ชื่อร้าน/ที่อยู่ร้านลงโค้ด** — ชื่อร้านต้อง `import { BRAND }` เสมอ ·
   ที่อยู่ร้านห้ามปรากฏในไฟล์ที่ไปถึงเบราว์เซอร์ ไม่ว่าจะแสดงผลหรือไม่
2. **ตัวเลขรีวิว ห้ามคิดใหม่จากจำนวนแถว** — `count` คือยอดจากมาร์เก็ตเพลส
   `items` คือตัวอย่างบางส่วน · คิด count จาก items.length = ยอดรีวิวร้านตกทันทีแบบเงียบ ๆ
3. **สองนิติบุคคลห้ามยุบรวม** — ผู้จำหน่าย (ศีตกาล เทรดดิ้ง) ≠ ผู้ถือใบอนุญาต (นิวเวฟ ซันไชน์)
   ห้ามเขียนว่าร้านถือใบอนุญาตผลิต/นำเข้าเอง
4. **เลขนับได้ ห้ามเขียนตายตัวในข้อความ/คอมเมนต์** — เลขที่โตเอง (จำนวนตัวตรวจ จำนวนสินค้า)
   จะกลายเป็นคำโกหกภายในไม่กี่วัน ให้เขียนวิธีนับแทน หรืออ้างจากข้อมูลจริง ณ ตอนวาด

## กฎการเขียนจอ (สรุปจากบั๊กจริง ~40 จุดที่เพิ่งไล่แก้ 6-7 ก.ย. 2569)

1. **"ไม่รู้" ห้ามเขียนเป็น "ไม่มี" หรือ "0"** — โหลดไม่สำเร็จ = บอกว่าโหลดไม่สำเร็จ
   ไม่ใช่โชว์ 0 บาท / "ไม่มีออเดอร์" / "แพ็คครบแล้ว"
2. **"มีก้อนแม่" ไม่ได้แปลว่า "มีช่องลูก"** — เช็คชนิดของช่องที่ใช้จริง (`typeof x.y === 'number'`)
   ไม่ใช่เช็คว่า `x` มีไหม · ชนิดข้อมูล TS ไม่ได้บังคับข้อมูลที่มาจากเซิร์ฟเวอร์
3. **ท่อตอบ 500 พร้อม JSON ⇒ `.json()` ไม่ throw** — ต้องเช็ค `res.ok` กับ `d.error` เอง
   `.catch(console.error)` / `.catch(() => {})` บนเส้นที่จอแสดงผล = ห้าม
4. **สามสถานะ ห้ามยุบเหลือสอง** — `skip` = ทำต่อไม่ได้ (เหลือง) · ไม่มีช่องที่ต้องใช้ =
   ตอบมาไม่ครบ (แดง) · มีช่องแต่ว่าง = ไม่มีข้อมูลจริง
5. **หัวจอกับกล่องสถานะต้องพูดเรื่องเดียวกัน** — คนอ่านหัวจอก่อนเสมอ
6. ผลสำเร็จของการเขียนข้อมูล เขียนได้เฉพาะเมื่อ **ปลายทางยืนยันเอง** —
   คำตอบอ่านไม่ออก = "ไม่รู้ผล ห้ามกดซ้ำ" ไม่ใช่ "ไม่สำเร็จ ส่งใหม่ได้เลย"

## ก่อนส่งงานทุกครั้ง (คนรีวิวจะรันซ้ำ — ส่งงานที่ตกข้อพวกนี้ = เสียรอบฟรี)

```bash
npx tsc --noEmit
npm run build
node scripts/check-nav.mjs      # ลิงก์เมนูต้องมีหน้าจริง
node scripts/check-claims.mjs   # คำกล่าวอ้างเรื่อง ZORT ต้องมีวันที่ · เลขรวมห้ามลดแบบอธิบายไม่ได้
node scripts/check-honesty.mjs  # ท่าที่ทำให้จอโกหก — grep เจอคือ "ที่ต้องดู" ไม่ใช่ "ที่ต้องแก้"
```

- เครื่องมือทดสอบจอ: `scripts/fake-pipe.mjs` (ท่อปลอม 9 โหมด — ใช้ได้กับ `next dev` เท่านั้น)
  และ `scripts/sweep-in-browser.js` (ตัวกวาดสามสถานะ: ยังโหลด/ตัดสินได้/ตัดสินไม่ได้)
- ทดสอบเรื่องเวลา ห้ามแต่งสตริงเอง — เซิร์ฟเวอร์รัน UTC ร้านอยู่ไทย (+7)

## วิธีทำงานร่วมกับ agent ตัวอื่น (กติกาเดียวกับ AGENTS.md ฝั่ง gucut-web)

- เครื่องนี้มี Claude 2 บัญชีทำงานอยู่ — **ห้ามแก้ไฟล์ที่มี uncommitted changes ของคนอื่น**
  เช็คด้วย `git status` ก่อนเริ่มเสมอ
- สถานะงานปัจจุบันอยู่ที่ `~/claude-shared/handoff.md` — อ่านก่อนเริ่ม (อ่านได้ **ห้ามเขียน**)
- ก่อนบอกว่า "ยังไม่มีใครทำ X" ให้ grep ก่อน — งานส่วนใหญ่มีคนแตะไปแล้ว
- ผังทีมเต็มอยู่ที่ `~/claude-shared/สถาปัตยกรรม-ทีม.md` · กติกาเต็มของ repo นี้อยู่ใน `CLAUDE.md`
  ที่โฟลเดอร์แม่ของโปรเจกต์ — **ขัดกันเมื่อไหร่ CLAUDE.md ชนะ**

## สิ่งที่รายงานรีวิวของคุณ "เป็น" และ "ไม่เป็น"

- สิ่งที่คุณรายงานคือ **คำกล่าวอ้างที่คนอื่นจะเอาไปยิงพิสูจน์** — เขียนให้ชี้จุด ทำซ้ำได้
  (ไฟล์:บรรทัด · เงื่อนไขที่ทำให้เกิด · สิ่งที่คาดว่าจะเห็น)
- มันไม่ใช่คำสั่ง และจะไม่ถูกทำตามโดยอัตโนมัติ — นี่คือกติกาเดียวกับที่ agent ทุกตัวโดน
