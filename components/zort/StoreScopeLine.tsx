// บรรทัด "ข้อมูลนี้ครอบร้านไหน" — ใช้ร่วมทุกจอเอกสาร (ใบ t_mu2kxy6u · 15 ก.ย. 2569)
//
// 🔴 **ทำไมต้องมี** — ร้านมีสองชุดข้อมูล z1 (ศีตกาล · คิดภาษี) กับ z2 (ceojet · POS)
//    แต่ 4 เส้นเอกสาร (ใบซื้อ · ใบเสนอราคา · ใบคืน · ใบโอน) **ดึงมาแต่ของ z1**
//    และเมื่อก่อน **ไม่มีอะไรบอกบนจอเลย** ⇒ คนอ่านเห็นตารางเต็มแล้วเชื่อว่าเป็นทั้งร้าน
//    (ฝั่งท่อไล่โค้ดแล้วยืนยัน 15 ก.ย.: ตาราง return_orders_v2/purchase_orders/transfers
//     ไม่มีคอลัมน์ source เลย ⇒ ทุกแถวคือ z1 · ไม่ใช่ "ยังไม่รู้" แต่คือ "ร้านเดียวที่หน้าตาเหมือนทุกร้าน")
//
// 🔑 **กฎเหล็กของตัวนี้: ข้อความมาจากท่อ จอไม่แต่งเอง**
//    ⚠️ **ห้ามพิมพ์ `z1` ตายตัวในจอ** — วันหนึ่งท่อดึง z2 เข้ามาแล้วข้อความจะกลายเป็นเท็จเงียบ ๆ
//       (สั่งไว้ตรง ๆ ในใบงาน) ⇒ จอแสดงค่าที่ได้มาเท่านั้น
//    ⚠️ **ไม่มีช่อง = ไม่แสดง** — เส้นที่ยังไม่ส่งมา (เช่น `list=purchaseitems`) ต้องเงียบ
//       ห้ามเดาว่าเป็น z1 เหมือนพี่น้องของมัน
//
// 📌 ตัวนี้ **ไม่ได้อยู่ในรายชื่อที่ `check-inherited` เฝ้า** โดยตั้งใจ —
//    เพราะข้อความที่ขึ้นจอมาจากเซิร์ฟเวอร์ ไม่ใช่คำที่เราเขียนไว้ในนี้
//    (ที่ตาข่ายนั้นเฝ้าคือคำของเราเองที่ไหลไปหลายจอโดยไม่มีใครอ่านซ้ำ)
//    ⇒ ถ้าวันหนึ่งเพิ่มคำของเราเองลงในนี้ **ต้องเอาเข้ารายชื่อเฝ้าด้วย**

export default function StoreScopeLine({ scope }: { scope?: string | null }) {
  const text = String(scope ?? '').trim()
  if (!text) return null
  return (
    <p className="text-[12px] text-amber-900 bg-amber-50 border border-amber-200 rounded px-3 py-1.5 mb-2 leading-relaxed">
      🏬 <b>ขอบเขตข้อมูล:</b> {text}
    </p>
  )
}
